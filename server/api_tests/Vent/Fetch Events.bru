meta {
  name: Fetch Events
  type: http
  seq: 6
}

get {
  url: {{base_URL}}/events
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
